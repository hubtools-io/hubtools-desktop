import { HubDBTableField } from '../FrameContext.types';

export const defaultHubDBTable = {
  name: 'default_hubdbtable',
  label: 'Default HubDB Table',
  required: false,
  locked: false,
  type: 'hubdbtable',
  default: 2010782,
} as HubDBTableField;
