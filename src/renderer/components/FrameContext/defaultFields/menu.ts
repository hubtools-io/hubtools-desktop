import { MenuField } from '../FrameContext.types';

export const defaultMenu = {
    name: 'default_menu',
    label: 'Default Menu',
    required: false,
    locked: false,
    type: 'menu',
    default: 12345678911,
} as MenuField;
