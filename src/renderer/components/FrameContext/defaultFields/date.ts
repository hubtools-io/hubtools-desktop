import { DateField } from '../FrameContext.types';

export const defaultDate = {
    name: 'default_date',
    label: 'Default Date',
    required: false,
    locked: false,
    type: 'date',
    default: 1577854800000,
} as DateField;
