import { GroupField } from '../FrameContext.types';

export const defaultGroup = {
    type: 'group',
    name: 'default_group',
    label: 'Default Group',
    expanded: true,
    children: [],
} as GroupField;
