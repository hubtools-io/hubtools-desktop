import { RichTextField } from '../FrameContext.types';

export const defaultRichText = {
    name: 'default_richtext',
    label: 'Default RichText',
    required: false,
    locked: false,
    type: 'richtext',
    default: null,
} as RichTextField;
