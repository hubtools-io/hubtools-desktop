import { CTAField } from '../FrameContext.types';

export const defaultCTA = {
  name: 'default_cta',
  label: 'Default CTA',
  required: false,
  locked: false,
  type: 'cta',
  default: null,
} as CTAField;
